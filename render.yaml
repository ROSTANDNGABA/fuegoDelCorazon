services:
  # Service Web Django
  - type: web
    name: fuegodelcorazon
    env: python
    plan: free
    buildCommand: ./build.sh
    startCommand: gunicorn student_matching.wsgi:application
    healthCheckPath: /
    
    # Variables d'environnement
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: False
      - key: PYTHON_VERSION
        value: 3.14.3
      - key: WEB_CONCURRENCY
        value: 1
      - key: DATABASE_URL
        fromDatabase:
          name: fuegodelcorazon-db
          property: connectionString
      # Variables PostgreSQL manuelles pour Render
      - key: RENDER_DB_NAME
        fromDatabase:
          name: fuegodelcorazon-db
          property: databaseName
      - key: RENDER_DB_USER
        fromDatabase:
          name: fuegodelcorazon-db
          property: user
      - key: RENDER_DB_PASSWORD
        fromDatabase:
          name: fuegodelcorazon-db
          property: password
      - key: RENDER_DB_HOST
        fromDatabase:
          name: fuegodelcorazon-db
          property: host
      - key: RENDER_DB_PORT
        fromDatabase:
          name: fuegodelcorazon-db
          property: port
        
    # Configuration de la base de données PostgreSQL
    databases:
      - name: fuegodelcorazon-db
        databaseName: fuegodelcorazon
        user: fuegodelcorazon

# Service de base de données PostgreSQL
databases:
  - name: fuegodelcorazon-db
    databaseName: fuegodelcorazon
    user: fuegodelcorazon
    plan: free
